# 🎨 Color Detection and Sorting App

A Streamlit app to detect and sort dominant colors from uploaded images or webcam photos.

## 🚀 Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
